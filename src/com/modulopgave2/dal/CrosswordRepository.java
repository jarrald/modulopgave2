package com.modulopgave2.dal;

import com.modulopgave2.model.Crossword;

import java.util.Collection;

public class CrosswordRepository extends RepositoryBase<Crossword> {

    @Override
    public Collection<Crossword> find(Crossword entity) throws Exception {
        return super.find(entity);
    }
}
